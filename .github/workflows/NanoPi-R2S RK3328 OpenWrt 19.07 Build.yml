name: NanoPi-R2S RK3328 OpenWrt 19.07 Build

env:
  SSH_ACTIONS:  false
  
on:
  push:
    paths:
      - 'CHANGELOG.md'
  watch:
    types: started

jobs:

  build:
    timeout-minutes: 720
    runs-on: ubuntu-18.04

    steps:

      - name: Checkout
        uses: actions/checkout@master
        with:
          ref: master

      - name: Initialize Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android/sdk
          sudo docker image prune -a -f
          sudo apt-get -y install unzip upx-ucl subversion
          sudo apt-get -y purge dotnet* ghc* google* llvm* mysql* php* zulu* firefox hhvm
          sudo apt-get -y autoremove --purge          
          wget -O - https://raw.githubusercontent.com/friendlyarm/build-env-on-ubuntu-bionic/master/install.sh | bash

      #- name: Setup Debug Session
      #  uses: P3TERX/debugger-action@master
  
      - name: Install Repo
        run: |
          git clone https://github.com/friendlyarm/repo
          sudo cp repo/repo /usr/bin/

      - name: Download Source
        run: |
          rm -rf code
          mkdir code
          cd code
          repo init -u https://github.com/friendlyarm/friendlywrt_manifests -b master-v19.07.5 -m rk3328.xml --repo-url=https://github.com/friendlyarm/repo --no-clone-bundle
          cp ../rk3328.xml ../code/.repo/manifests/rk3328.xml
          repo sync -c --no-clone-bundle -j8

      - name: Merge LEDE
        run: |
          cd code/friendlywrt
          echo 'src-git packages https://git.openwrt.org/feed/packages.git;openwrt-19.07 > feeds.conf.default
          echo 'src-git luci https://git.openwrt.org/project/luci.git;openwrt-19.07' >> feeds.conf.default
          echo 'src-git routing https://git.openwrt.org/feed/routing.git;openwrt-19.07' >> feeds.conf.default
          echo 'src-git telephony https://git.openwrt.org/feed/telephony.git;openwrt-19.07' >> feeds.conf.default
          
          #echo 'src-git routing https://git.openwrt.org/feed/routing.git^02b4dbfcb7b8f8b566940847d22d5a6f229d2e66' > feeds.conf.default
          #echo 'src-git telephony https://git.openwrt.org/feed/telephony.git^6e019c94d0fa7162548d528bf4ba060a61f8cb59' >> feeds.conf.default
          #echo 'src-git freifunk https://github.com/freifunk/openwrt-packages.git^73454e6fe1bdc902c69ca0079d979fdb94d6fb1f' >> feeds.conf.default
          #echo 'src-git packages https://github.com/friendlyarm/friendlywrt_project_packages^024097a846aa1a7ec5d0d2182d035584260bb80f ' >> feeds.conf.default
          #echo 'src-git luci https://github.com/friendlyarm/friendlywrt_project_luci^85ff2c9e1274c49e1c8e0b92a244a9ddb14b54d9' >> feeds.conf.default

      - name: Processing drivers
        run: |
          cd code/friendlywrt/package
          # git clone  https://github.com/fw876/helloworld custom/helloworld
          #new drivers:
          #svn co https://github.com/coolsnowwolf/lede/trunk/package/lean/r8125 custom/r8125
          #svn co https://github.com/coolsnowwolf/lede/trunk/package/lean/r8152 custom/r8152
          #svn co https://github.com/coolsnowwolf/lede/trunk/package/lean/r8168 custom/r8168
          #svn co https://github.com/immortalwrt/immortalwrt/trunk/package/kernel/rtl8812au-ac custom/rtl8812au-ac 
          #svn co https://github.com/immortalwrt/immortalwrt/trunk/package/kernel/rtl8821cu custom/rtl8821cu
          #svn co https://github.com/immortalwrt/immortalwrt/trunk/package/kernel/rtl8188eu custom/rtl8188eu
          #svn co https://github.com/immortalwrt/immortalwrt/trunk/package/kernel/rtl8192du custom/rtl8192du
          #svn co https://github.com/immortalwrt/immortalwrt/trunk/package/kernel/rtl88x2bu custom/rtl88x2bu
          
          
          
          # 花生壳内网穿透
          #svn co https://github.com/teasiu/dragino2/trunk/devices/common/diy/package/teasiu/luci-app-phtunnel custom/luci-app-phtunnel
          #svn co https://github.com/teasiu/dragino2/trunk/devices/common/diy/package/teasiu/phtunnel custom/phtunnel
          #svn co https://github.com/QiuSimons/dragino2-teasiu/trunk/package/teasiu/luci-app-oray custom/luci-app-oray

      - name: Update Target.mk
        run: |
          cd code/friendlywrt/include
          sed -i 's/dnsmasq /dnsmasq-full default-settings luci /' target.mk
          
          
      - name: Update Feeds		
        run: |		
          cd code/friendlywrt		
          ./scripts/feeds update -a		
          ./scripts/feeds install -a

      - name: Custom Configure Files
        run: |
          rm -f code/friendlywrt/.config*
          cp r2s-rk3328-config code/configs/config_rk3328
          # cat r2s-rk3328-config > code/friendlywr/.config
        
      - name: Set Default Values
        run: |
          cd code/friendlywrt
          #echo -e "\nBuild date:$(date +%Y-%m-%d), by dongyusen@gmail.com\n" >> package/base-files/files/etc/banner
           
      - name: Install UPX
        run: |
          #wget -q -O - https://github.com/upx/upx/releases/download/v3.96/upx-3.96-amd64_linux.tar.xz | tar -Jx --strip 1 -f - -C code/friendlywrt/staging_dir/host/bin/

      - name: SSH连接
        uses: DONGYUSEN/debugger-action@master
        if: env.SSH_ACTIONS == 'true'

      - name: Build OpenWrt
        run: |
          cd code
          
          ## manully compiler
          #cd friendlywrt   
          #make defconfig
          #make download -j8
          #make -j$(($(nproc) + 1)) || make -j$(($(nproc) + 1))  V=s
          #time make V=s VERBOSE=1 -j$(nproc) JOBS=$(nproc) 2>&1 | tee build.log | egrep -i '(warn|error)'
          
          sed -i 's/make download -j$(nproc)/make download -j2 V=s/'  build.sh
          sed -i '/feeds/d' scripts/mk-friendlywrt.sh          
          sed -i 's/set -eu/set -u/' scripts/mk-friendlywrt.sh
          sed -i 's/make download -j$(nproc)/make download -j$(nproc) V=s/' scripts/mk-friendlywrt.sh
          echo -e "$(nproc+1) thread compile"
          sed -i 's/make -j$(nproc) V=s/make -j1 V=s/' scripts/mk-friendlywrt.sh
          # sed -i '/make -j1 V=s/d' scripts/mk-friendlywrt.sh
          ./build.sh nanopi_r2s.mk
          

      - name: Fix Rootfs Owner and Group
        run: |
          sudo df -lh
          lodev=$(sudo losetup -f)
          echo "found unused loop dev $lodev"
          sudo losetup -o 100663296 $lodev code/out/*.img
          #sudo mkfs -t ext4 $lodev
          sudo rm -rf /mnt/friendlywrt-tmp
          sudo mkdir -p /mnt/friendlywrt-tmp
          sudo mount $lodev /mnt/friendlywrt-tmp
          sudo chown -R root:root /mnt/friendlywrt-tmp
          sudo umount /mnt/friendlywrt-tmp
          sudo losetup -d $lodev

      - name: Assemble Artifact
        run: |
          rm -rf ./artifact/
          mkdir -p ./artifact/
          
          #mkdir -p ./artifact/others
          #cp -a code/bin/targets/*/* ./artifact/other
          
          cp code/out/*.* ./artifact/
          cp code/friendlywrt/.config ./artifact/

      - name: Upload Artifact
        uses: actions/upload-artifact@master
        with:
          name: FriendlyWrt_NanoPi-R2S RK3328 v19.07 make build.sh
          path: ./artifact/
